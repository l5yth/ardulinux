[platformio]
default_envs = ardulinux

[env:ardulinux]
# symlink://../ points at the platform repo root for local development.
# Replace with the git URL when consuming from outside the repo.
platform = symlink://../
framework = arduino
board = ardulinux
build_flags = ${env.build_flags} -O0




